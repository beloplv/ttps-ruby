<p style="color: green"><%= notice %></p>

<h1>Usuarios</h1>

<div id="usuarios">
  <% @usuarios.each do |usuario| %>
    <%= render usuario %>
    <p>
      <%= link_to "Show this usurio", usuario %>
    </p>
  <% end %>
</div>

<%= link_to "New usuario", new_usuario_path %>