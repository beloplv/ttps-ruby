<p>

<% if current_user.role == "administrator" || current_user.role == "employee" %>
    <% if can? :read, User %>
        <%= link_to "Users", users_path %>
    <% end %>
<% end %>
<% if can? :read, BranchOffice %>
  <%= link_to "Branch Offices", branch_offices_path %>
<% end %>
<% if can? :manage, Schedule %>
  <%= link_to "Schedules", schedules_path %>
<% end %>
<% if can? :read, Locality %>
  <%= link_to "Localities", localities_path %>
<% end %>
<% if can? :read, Turn %>
  <%= link_to "Turns", turns_path %>
<% end %>

<%= link_to "My profile", user_path(:id => current_user.id)%>
<%= link_to "Sign out", destroy_user_session_path%> 
</p>

<h1>Home</h1>

<p>Hello <%= current_user.email %>!</p>
<p>You are a <%= current_user.role %></p>


    <p>
        You have succesfully authenticated with your Devise account.
        You are now ready to interact with this application.
    </p>

